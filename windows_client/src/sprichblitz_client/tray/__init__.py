"""Tray-Icon-Komponenten (pystray-basiert).

Lazy-Imports: ``pystray`` und ``PIL`` werden erst beim Aufruf der
Klassenmethoden importiert, damit dieses Paket auch auf macOS-Dev
ohne installierte GUI-Stacks importierbar bleibt.
"""
